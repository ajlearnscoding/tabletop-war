public class MapEditor {
    private int[][] map;

    public MapEditor(int rows, int columns) {
        map = new int[rows][columns];
    }

    // Define the methods to interact with the map here
    
}
