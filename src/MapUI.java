import javax.swing.*;

public class MapUI extends JFrame {
	// Define the GUI components and layout here

	public MapUI() {
		super("Map Editor");
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setSize(800, 600);
		setLocationRelativeTo(null);

		// Add the GUI components to the window here

	}

	// Define the methods that interact with the UI components here

}
